// <auto-generated/> by Diplomat

using System;
using System.Runtime.InteropServices;
using Devolutions.Picky.Diplomat;

namespace Devolutions.Picky;

#nullable enable

public partial class SignatureAlgorithm: IDisposable
{
    private unsafe RustHandle<Raw.SignatureAlgorithm> _inner;

    /// <summary>
    /// Roots the wrappers this value borrows from so the GC cannot finalize
    /// a borrowed-from parent while this value is alive.
    /// </summary>
    private object[] _edges;

    private static readonly unsafe RustDestructor<Raw.SignatureAlgorithm> _destroy = Raw.SignatureAlgorithm.Destroy;

    /// <summary>
    /// Creates a managed <c>SignatureAlgorithm</c> from a raw handle.
    /// </summary>
    /// <remarks>
    /// Safety: you should not build two managed objects using the same raw handle (may cause use-after-free and double-free).
    /// <br/>
    /// This constructor assumes the raw struct is allocated on Rust side.
    /// If implemented, the custom Drop implementation on Rust side WILL run on destruction.
    /// </remarks>
    internal unsafe SignatureAlgorithm(Raw.SignatureAlgorithm* handle)
    {
        _inner = RustHandle<Raw.SignatureAlgorithm>.Owned(handle, _destroy);
        _edges = System.Array.Empty<object>();
    }

    /// <remarks>
    /// Edges only keep the borrowed-from objects GC-reachable. Explicitly
    /// <c>Dispose</c>-ing a parent while a borrowing child is in use is still a
    /// use-after-free and remains the caller's responsibility.
    /// </remarks>
    internal unsafe SignatureAlgorithm(Raw.SignatureAlgorithm* handle, object[] edges)
    {
        _inner = RustHandle<Raw.SignatureAlgorithm>.Owned(handle, _destroy);
        _edges = edges;
    }

    /// <summary>
    /// Wraps a handle that already knows whether it owns the pointer. A
    /// borrowed return passes a non-owning handle, so Dispose and the finalizer
    /// leave Rust's pointer alone; the edges keep the borrowed-from owners alive
    /// while this view is in use.
    /// </summary>
    internal unsafe SignatureAlgorithm(RustHandle<Raw.SignatureAlgorithm> inner, object[] edges)
    {
        _inner = inner;
        _edges = edges;
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>SignatureAlgorithm</c> allocated on Rust side.
    /// </returns>
    public static SignatureAlgorithm NewRsaPkcs1v15(HashAlgorithm hashAlgorithm)
    {
        unsafe
        {
            var result = Raw.SignatureAlgorithm.NewRsaPkcs1v15(hashAlgorithm);
            if (!result.IsOk)
            {
                throw new PickyException(new PickyError(result.Err));
            }
            return new SignatureAlgorithm(result.Ok);
        }
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>SignatureAlgorithm</c> allocated on Rust side.
    /// </returns>
    public static SignatureAlgorithm NewEcdsa(HashAlgorithm hashAlgorithm)
    {
        unsafe
        {
            var result = Raw.SignatureAlgorithm.NewEcdsa(hashAlgorithm);
            if (!result.IsOk)
            {
                throw new PickyException(new PickyError(result.Err));
            }
            return new SignatureAlgorithm(result.Ok);
        }
    }

    /// <exception cref="PickyException"></exception>
    public void Verify(PublicKey publicKey, byte[] msg, byte[] signature)
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                throw new ObjectDisposedException("SignatureAlgorithm");
            }
            if (publicKey == null) throw new ArgumentNullException(nameof(publicKey));
            Raw.PublicKey* publicKeyRaw = publicKey.AsFFI();
            if (publicKeyRaw == null) throw new ObjectDisposedException(nameof(PublicKey));
            if (msg == null) throw new ArgumentNullException(nameof(msg));
            if (signature == null) throw new ArgumentNullException(nameof(signature));
            fixed (byte* msgPtr = msg)
            fixed (byte* signaturePtr = signature)
            {
                var result = Raw.SignatureAlgorithm.Verify(AsFFI(), publicKeyRaw, new DiplomatSliceU8 { Ptr = msgPtr, Len = (nuint)msg.Length }, new DiplomatSliceU8 { Ptr = signaturePtr, Len = (nuint)signature.Length });
                GC.KeepAlive(this);
                GC.KeepAlive(publicKey);
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return;
            }
        }
    }

    /// <summary>
    /// Returns the underlying raw handle.
    /// </summary>
    internal unsafe Raw.SignatureAlgorithm* AsFFI()
    {
        return _inner.Ptr;
    }

    /// <summary>
    /// Destroys the underlying object immediately.
    /// </summary>
    public void Dispose()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                return;
            }

            _inner.Release();
            _inner = default;
            _edges = System.Array.Empty<object>(); // release refs so borrowed-from owners can be GC'd

            GC.SuppressFinalize(this);
        }
    }

    ~SignatureAlgorithm()
    {
        Dispose();
    }
}