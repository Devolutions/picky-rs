// <auto-generated/> by Diplomat

using System;
using System.Runtime.InteropServices;
using Devolutions.Picky.Diplomat;

namespace Devolutions.Picky;

#nullable enable

public partial class Pkcs12CryptoContext: IDisposable
{
    private unsafe RustHandle<Raw.Pkcs12CryptoContext> _inner;

    /// <summary>
    /// Roots the wrappers this value borrows from so the GC cannot finalize
    /// a borrowed-from parent while this value is alive.
    /// </summary>
    private object[] _edges;

    private static readonly unsafe RustDestructor<Raw.Pkcs12CryptoContext> _destroy = Raw.Pkcs12CryptoContext.Destroy;

    /// <summary>
    /// Creates a managed <c>Pkcs12CryptoContext</c> from a raw handle.
    /// </summary>
    /// <remarks>
    /// Safety: you should not build two managed objects using the same raw handle (may cause use-after-free and double-free).
    /// <br/>
    /// This constructor assumes the raw struct is allocated on Rust side.
    /// If implemented, the custom Drop implementation on Rust side WILL run on destruction.
    /// </remarks>
    internal unsafe Pkcs12CryptoContext(Raw.Pkcs12CryptoContext* handle)
    {
        _inner = RustHandle<Raw.Pkcs12CryptoContext>.Owned(handle, _destroy);
        _edges = System.Array.Empty<object>();
    }

    /// <remarks>
    /// Edges only keep the borrowed-from objects GC-reachable. Explicitly
    /// <c>Dispose</c>-ing a parent while a borrowing child is in use is still a
    /// use-after-free and remains the caller's responsibility.
    /// </remarks>
    internal unsafe Pkcs12CryptoContext(Raw.Pkcs12CryptoContext* handle, object[] edges)
    {
        _inner = RustHandle<Raw.Pkcs12CryptoContext>.Owned(handle, _destroy);
        _edges = edges;
    }

    /// <summary>
    /// Wraps a handle that already knows whether it owns the pointer. A
    /// borrowed return passes a non-owning handle, so Dispose and the finalizer
    /// leave Rust's pointer alone; the edges keep the borrowed-from owners alive
    /// while this view is in use.
    /// </summary>
    internal unsafe Pkcs12CryptoContext(RustHandle<Raw.Pkcs12CryptoContext> inner, object[] edges)
    {
        _inner = inner;
        _edges = edges;
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>Pkcs12CryptoContext</c> allocated on Rust side.
    /// </returns>
    public static Pkcs12CryptoContext WithPassword(string password)
    {
        unsafe
        {
            if (password == null) throw new ArgumentNullException(nameof(password));
            byte[] passwordBytes = System.Text.Encoding.UTF8.GetBytes(password);
            fixed (byte* passwordPtr = passwordBytes)
            {
                var result = Raw.Pkcs12CryptoContext.WithPassword(new DiplomatSliceU8 { Ptr = passwordPtr, Len = (nuint)passwordBytes.Length });
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return new Pkcs12CryptoContext(result.Ok);
            }
        }
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>Pkcs12CryptoContext</c> allocated on Rust side.
    /// </returns>
    public static Pkcs12CryptoContext NoPassword()
    {
        unsafe
        {
            var result = Raw.Pkcs12CryptoContext.NoPassword();
            if (!result.IsOk)
            {
                throw new PickyException(new PickyError(result.Err));
            }
            return new Pkcs12CryptoContext(result.Ok);
        }
    }

    /// <summary>
    /// Returns the underlying raw handle.
    /// </summary>
    internal unsafe Raw.Pkcs12CryptoContext* AsFFI()
    {
        return _inner.Ptr;
    }

    /// <summary>
    /// Destroys the underlying object immediately.
    /// </summary>
    public void Dispose()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                return;
            }

            _inner.Release();
            _inner = default;
            _edges = System.Array.Empty<object>(); // release refs so borrowed-from owners can be GC'd

            GC.SuppressFinalize(this);
        }
    }

    ~Pkcs12CryptoContext()
    {
        Dispose();
    }
}