// <auto-generated/> by Diplomat

using System;
using System.Runtime.InteropServices;
using Devolutions.Picky.Diplomat;

namespace Devolutions.Picky;

#nullable enable

public partial class Pem: IDisposable
{
    private unsafe RustHandle<Raw.Pem> _inner;

    /// <summary>
    /// Roots the wrappers this value borrows from so the GC cannot finalize
    /// a borrowed-from parent while this value is alive.
    /// </summary>
    private object[] _edges;

    private static readonly unsafe RustDestructor<Raw.Pem> _destroy = Raw.Pem.Destroy;

    public ulong DataLength
    {
        get
        {
            return GetDataLength();
        }
    }

    public string Label
    {
        get
        {
            return GetLabel();
        }
    }

    /// <summary>
    /// Creates a managed <c>Pem</c> from a raw handle.
    /// </summary>
    /// <remarks>
    /// Safety: you should not build two managed objects using the same raw handle (may cause use-after-free and double-free).
    /// <br/>
    /// This constructor assumes the raw struct is allocated on Rust side.
    /// If implemented, the custom Drop implementation on Rust side WILL run on destruction.
    /// </remarks>
    internal unsafe Pem(Raw.Pem* handle)
    {
        _inner = RustHandle<Raw.Pem>.Owned(handle, _destroy);
        _edges = System.Array.Empty<object>();
    }

    /// <remarks>
    /// Edges only keep the borrowed-from objects GC-reachable. Explicitly
    /// <c>Dispose</c>-ing a parent while a borrowing child is in use is still a
    /// use-after-free and remains the caller's responsibility.
    /// </remarks>
    internal unsafe Pem(Raw.Pem* handle, object[] edges)
    {
        _inner = RustHandle<Raw.Pem>.Owned(handle, _destroy);
        _edges = edges;
    }

    /// <summary>
    /// Wraps a handle that already knows whether it owns the pointer. A
    /// borrowed return passes a non-owning handle, so Dispose and the finalizer
    /// leave Rust's pointer alone; the edges keep the borrowed-from owners alive
    /// while this view is in use.
    /// </summary>
    internal unsafe Pem(RustHandle<Raw.Pem> inner, object[] edges)
    {
        _inner = inner;
        _edges = edges;
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>Pem</c> allocated on Rust side.
    /// </returns>
    public static Pem New(string label, byte[] data)
    {
        unsafe
        {
            if (label == null) throw new ArgumentNullException(nameof(label));
            if (data == null) throw new ArgumentNullException(nameof(data));
            byte[] labelBytes = System.Text.Encoding.UTF8.GetBytes(label);
            fixed (byte* labelPtr = labelBytes)
            fixed (byte* dataPtr = data)
            {
                var result = Raw.Pem.New(new DiplomatSliceU8 { Ptr = labelPtr, Len = (nuint)labelBytes.Length }, new DiplomatSliceU8 { Ptr = dataPtr, Len = (nuint)data.Length });
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return new Pem(result.Ok);
            }
        }
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>Pem</c> allocated on Rust side.
    /// </returns>
    public static Pem LoadFromFile(string path)
    {
        unsafe
        {
            if (path == null) throw new ArgumentNullException(nameof(path));
            byte[] pathBytes = System.Text.Encoding.UTF8.GetBytes(path);
            fixed (byte* pathPtr = pathBytes)
            {
                var result = Raw.Pem.LoadFromFile(new DiplomatSliceU8 { Ptr = pathPtr, Len = (nuint)pathBytes.Length });
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return new Pem(result.Ok);
            }
        }
    }

    /// <exception cref="PickyException"></exception>
    public void SaveToFile(string path)
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                throw new ObjectDisposedException("Pem");
            }
            if (path == null) throw new ArgumentNullException(nameof(path));
            byte[] pathBytes = System.Text.Encoding.UTF8.GetBytes(path);
            fixed (byte* pathPtr = pathBytes)
            {
                var result = Raw.Pem.SaveToFile(AsFFI(), new DiplomatSliceU8 { Ptr = pathPtr, Len = (nuint)pathBytes.Length });
                GC.KeepAlive(this);
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return;
            }
        }
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>Pem</c> allocated on Rust side.
    /// </returns>
    public static Pem Parse(string input)
    {
        unsafe
        {
            if (input == null) throw new ArgumentNullException(nameof(input));
            byte[] inputBytes = System.Text.Encoding.UTF8.GetBytes(input);
            fixed (byte* inputPtr = inputBytes)
            {
                var result = Raw.Pem.Parse(new DiplomatSliceU8 { Ptr = inputPtr, Len = (nuint)inputBytes.Length });
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return new Pem(result.Ok);
            }
        }
    }

    public ulong GetDataLength()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                throw new ObjectDisposedException("Pem");
            }
            var result = Raw.Pem.GetDataLength(AsFFI());
            GC.KeepAlive(this);
            return result;
        }
    }

    /// <exception cref="PickyException"></exception>
    public string GetLabel()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                throw new ObjectDisposedException("Pem");
            }
            DiplomatWrite writeable = new DiplomatWrite();
            try
            {
                var result = Raw.Pem.GetLabel(AsFFI(), &writeable);
                GC.KeepAlive(this);
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return writeable.ToUnicode();
            }
            finally
            {
                writeable.Dispose();
            }
        }
    }

    /// <exception cref="PickyException"></exception>
    public string ToRepr()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                throw new ObjectDisposedException("Pem");
            }
            DiplomatWrite writeable = new DiplomatWrite();
            try
            {
                var result = Raw.Pem.ToRepr(AsFFI(), &writeable);
                GC.KeepAlive(this);
                if (!result.IsOk)
                {
                    throw new PickyException(new PickyError(result.Err));
                }
                return writeable.ToUnicode();
            }
            finally
            {
                writeable.Dispose();
            }
        }
    }

    /// <summary>
    /// Returns the underlying raw handle.
    /// </summary>
    internal unsafe Raw.Pem* AsFFI()
    {
        return _inner.Ptr;
    }

    /// <summary>
    /// Destroys the underlying object immediately.
    /// </summary>
    public void Dispose()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                return;
            }

            _inner.Release();
            _inner = default;
            _edges = System.Array.Empty<object>(); // release refs so borrowed-from owners can be GC'd

            GC.SuppressFinalize(this);
        }
    }

    ~Pem()
    {
        Dispose();
    }
}