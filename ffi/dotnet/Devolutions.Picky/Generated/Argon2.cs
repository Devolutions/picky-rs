// <auto-generated/> by Diplomat

using System;
using System.Runtime.InteropServices;
using Devolutions.Picky.Diplomat;

namespace Devolutions.Picky;

#nullable enable

public partial class Argon2: IDisposable
{
    private unsafe RustHandle<Raw.Argon2> _inner;

    /// <summary>
    /// Roots the wrappers this value borrows from so the GC cannot finalize
    /// a borrowed-from parent while this value is alive.
    /// </summary>
    private object[] _edges;

    private static readonly unsafe RustDestructor<Raw.Argon2> _destroy = Raw.Argon2.Destroy;

    /// <summary>
    /// Creates a managed <c>Argon2</c> from a raw handle.
    /// </summary>
    /// <remarks>
    /// Safety: you should not build two managed objects using the same raw handle (may cause use-after-free and double-free).
    /// <br/>
    /// This constructor assumes the raw struct is allocated on Rust side.
    /// If implemented, the custom Drop implementation on Rust side WILL run on destruction.
    /// </remarks>
    internal unsafe Argon2(Raw.Argon2* handle)
    {
        _inner = RustHandle<Raw.Argon2>.Owned(handle, _destroy);
        _edges = System.Array.Empty<object>();
    }

    /// <remarks>
    /// Edges only keep the borrowed-from objects GC-reachable. Explicitly
    /// <c>Dispose</c>-ing a parent while a borrowing child is in use is still a
    /// use-after-free and remains the caller's responsibility.
    /// </remarks>
    internal unsafe Argon2(Raw.Argon2* handle, object[] edges)
    {
        _inner = RustHandle<Raw.Argon2>.Owned(handle, _destroy);
        _edges = edges;
    }

    /// <summary>
    /// Wraps a handle that already knows whether it owns the pointer. A
    /// borrowed return passes a non-owning handle, so Dispose and the finalizer
    /// leave Rust's pointer alone; the edges keep the borrowed-from owners alive
    /// while this view is in use.
    /// </summary>
    internal unsafe Argon2(RustHandle<Raw.Argon2> inner, object[] edges)
    {
        _inner = inner;
        _edges = edges;
    }

    /// <exception cref="PickyException"></exception>
    /// <returns>
    /// A <c>Argon2</c> allocated on Rust side.
    /// </returns>
    public static Argon2 New(Argon2Algorithm algorithm, Argon2Params parameters)
    {
        unsafe
        {
            if (parameters == null) throw new ArgumentNullException(nameof(parameters));
            Raw.Argon2Params* parametersRaw = parameters.AsFFI();
            if (parametersRaw == null) throw new ObjectDisposedException(nameof(Argon2Params));
            var result = Raw.Argon2.New(algorithm, parametersRaw);
            GC.KeepAlive(parameters);
            if (!result.IsOk)
            {
                throw new PickyException(new PickyError(result.Err));
            }
            return new Argon2(result.Ok);
        }
    }

    /// <exception cref="PickyException"></exception>
    public string HashPassword(string password)
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                throw new ObjectDisposedException("Argon2");
            }
            if (password == null) throw new ArgumentNullException(nameof(password));
            byte[] passwordBytes = System.Text.Encoding.UTF8.GetBytes(password);
            fixed (byte* passwordPtr = passwordBytes)
            {
                DiplomatWrite writeable = new DiplomatWrite();
                try
                {
                    var result = Raw.Argon2.HashPassword(AsFFI(), new DiplomatSliceU8 { Ptr = passwordPtr, Len = (nuint)passwordBytes.Length }, &writeable);
                    GC.KeepAlive(this);
                    if (!result.IsOk)
                    {
                        throw new PickyException(new PickyError(result.Err));
                    }
                    return writeable.ToUnicode();
                }
                finally
                {
                    writeable.Dispose();
                }
            }
        }
    }

    /// <summary>
    /// Returns the underlying raw handle.
    /// </summary>
    internal unsafe Raw.Argon2* AsFFI()
    {
        return _inner.Ptr;
    }

    /// <summary>
    /// Destroys the underlying object immediately.
    /// </summary>
    public void Dispose()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                return;
            }

            _inner.Release();
            _inner = default;
            _edges = System.Array.Empty<object>(); // release refs so borrowed-from owners can be GC'd

            GC.SuppressFinalize(this);
        }
    }

    ~Argon2()
    {
        Dispose();
    }
}