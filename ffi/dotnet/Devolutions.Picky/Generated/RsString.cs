// <auto-generated/> by Diplomat

using System;
using System.Runtime.InteropServices;
using Devolutions.Picky.Diplomat;

namespace Devolutions.Picky;

#nullable enable

public partial class RsString: IDisposable
{
    private unsafe RustHandle<Raw.RsString> _inner;

    /// <summary>
    /// Roots the wrappers this value borrows from so the GC cannot finalize
    /// a borrowed-from parent while this value is alive.
    /// </summary>
    private object[] _edges;

    private static readonly unsafe RustDestructor<Raw.RsString> _destroy = Raw.RsString.Destroy;

    /// <summary>
    /// Creates a managed <c>RsString</c> from a raw handle.
    /// </summary>
    /// <remarks>
    /// Safety: you should not build two managed objects using the same raw handle (may cause use-after-free and double-free).
    /// <br/>
    /// This constructor assumes the raw struct is allocated on Rust side.
    /// If implemented, the custom Drop implementation on Rust side WILL run on destruction.
    /// </remarks>
    internal unsafe RsString(Raw.RsString* handle)
    {
        _inner = RustHandle<Raw.RsString>.Owned(handle, _destroy);
        _edges = System.Array.Empty<object>();
    }

    /// <remarks>
    /// Edges only keep the borrowed-from objects GC-reachable. Explicitly
    /// <c>Dispose</c>-ing a parent while a borrowing child is in use is still a
    /// use-after-free and remains the caller's responsibility.
    /// </remarks>
    internal unsafe RsString(Raw.RsString* handle, object[] edges)
    {
        _inner = RustHandle<Raw.RsString>.Owned(handle, _destroy);
        _edges = edges;
    }

    /// <summary>
    /// Wraps a handle that already knows whether it owns the pointer. A
    /// borrowed return passes a non-owning handle, so Dispose and the finalizer
    /// leave Rust's pointer alone; the edges keep the borrowed-from owners alive
    /// while this view is in use.
    /// </summary>
    internal unsafe RsString(RustHandle<Raw.RsString> inner, object[] edges)
    {
        _inner = inner;
        _edges = edges;
    }

    /// <returns>
    /// A <c>RsString</c> allocated on Rust side.
    /// </returns>
    public static RsString FromString(string s)
    {
        unsafe
        {
            if (s == null) throw new ArgumentNullException(nameof(s));
            byte[] sBytes = System.Text.Encoding.UTF8.GetBytes(s);
            fixed (byte* sPtr = sBytes)
            {
                Raw.RsString* result = Raw.RsString.FromString(new DiplomatSliceU8 { Ptr = sPtr, Len = (nuint)sBytes.Length });
                return new RsString(result);
            }
        }
    }

    /// <summary>
    /// Returns the underlying raw handle.
    /// </summary>
    internal unsafe Raw.RsString* AsFFI()
    {
        return _inner.Ptr;
    }

    /// <summary>
    /// Destroys the underlying object immediately.
    /// </summary>
    public void Dispose()
    {
        unsafe
        {
            if (_inner.IsNull)
            {
                return;
            }

            _inner.Release();
            _inner = default;
            _edges = System.Array.Empty<object>(); // release refs so borrowed-from owners can be GC'd

            GC.SuppressFinalize(this);
        }
    }

    ~RsString()
    {
        Dispose();
    }
}